#!/bin/sh
# import common lib for syntax
. lib

hostname="${b}$(hostname)${d}"
public_ip="${a}$(dig +short myip.opendns.com @resolver1.opendns.com)${d}"
output="| ${hostname} | ${public_ip} "

printf -- "$output"
