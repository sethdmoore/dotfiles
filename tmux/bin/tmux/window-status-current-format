#!/bin/sh
# import common lib for syntax
. tmux-lib

output="[-${aa}#{?window_zoomed_flag,${i} ,  }"
output="${output}#{window_name}"
output="${output}#{?window_zoomed_flag, ${i},  }${d}-]"

printf -- "$output"
