#!/bin/sh

function main() {
  local mode stamp
  mode="$1"

  if [ "$mode" = "--clipboard" ]; then
    maim -s | xclip -selection clipboard -t image/png
  else
    stamp=$(date -u +"%Y-%m-%dT%H_%M_%SZ")
    maim -s "$PWD/maim-$stamp.png"
  fi
}

main $@
